[package]
name = "sysmon"
version = "0.0.0"
edition = "2024"
rust-version = "1.88"
publish = false
license = "MIT OR Apache-2.0"
description = "A live system monitor built on malevich — not published"

[dependencies]
malevich = { path = "../..", features = ["ratatui"] }
ratatui = "0.30"
crossterm = "0.29"
sysinfo = "0.33"
